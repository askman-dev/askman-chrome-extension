import { DialogPanel } from '@src/components/features/dialog/DialogPanel';

// This is a compatibility wrapper for backwards compatibility
// All functionality has been moved to the DialogPanel component
export default DialogPanel;
